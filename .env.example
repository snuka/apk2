# OpenAI API Key (required)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Railway Environment Variables
# These are automatically set by Railway:
# PORT - The port your app should listen on
# RAILWAY_ENVIRONMENT - The environment name (production, etc.)

# Optional: Node environment
NODE_ENV=production

# Google OAuth2 Credentials (for Calendar integration)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Google Calendar ID (optional, defaults to 'primary')
GOOGLE_CALENDAR_ID=primary

# Encryption key for token storage (generate a random string)
TOKEN_ENCRYPTION_KEY=your-random-encryption-key-here
